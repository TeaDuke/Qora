#pragma once

#include <QtWidgets>
#include <QtCore>
#include <QtGui>


class MainWindow : public QWidget
{
    Q_OBJECT

public:
    MainWindow(QWidget* parent = nullptr);
    ~MainWindow();

private:

};
